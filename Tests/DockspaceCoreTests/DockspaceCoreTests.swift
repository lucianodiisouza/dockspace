import XCTest
@testable import DockspaceCore

final class DockspaceCoreTests: XCTestCase {
    func testBootstrapSucceeds() {
        // Placeholder test. Replaced in Phase 1 with real coverage of
        // DockPlistReader / DockPlistWriter roundtrips.
        XCTAssertTrue(true)
    }
}
